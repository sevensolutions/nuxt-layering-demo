{
  "name": "nuxt-layering-demo",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "(cd layers/base-layer && npm i && npm run dev:prepare); (cd layers/user-management && npm i && npm run dev:prepare); (cd layers/custom-user-management && npm i && npm run dev:prepare); (cd final && npm i && npm run dev:prepare);",
    "dev": "cd final && npm run dev"
  }
}
